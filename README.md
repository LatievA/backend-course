# Assignment 3

## How to run

```bash
npm install # install dependencies if you haven't installed them yet

npm start # starts the node server
```
Now you can send requests to the localhost:3000

## API Endpoints

### Workout Endpoints

`POST api/workouts`         Create a new workout

`GET api/workouts`          Get all workouts (without limiting !)

`GET api/workouts/:id`      Get one workout by id

`PUT api/workouts/:id`      Update one workout by id

`DELETE api/workouts/:id`   Delete one workout by id

### Exercise Endpoints

`POST api/exercises`         Create a new exercise

`GET api/exercises`          Get all exercises (also without limiting !)

`GET api/exercises/:id`      Get one exercise by id

`PUT api/exercises/:id`      Update one exercise by id

`DELETE api/exercises:id`    Delete one exercise by id

## Examples of requests

![Снимок экрана (267).png](./images/Снимок%20экрана%20(267).png)
![Снимок экрана (268).png](./images/Снимок%20экрана%20(268).png)
![Снимок экрана (269).png](./images/Снимок%20экрана%20(269).png)
![Снимок экрана (270).png](./images/Снимок%20экрана%20(270).png)
![Снимок экрана (271).png](./images/Снимок%20экрана%20(271).png)
![Снимок экрана (272).png](./images/Снимок%20экрана%20(272).png)
![Снимок экрана (273).png](./images/Снимок%20экрана%20(273).png)
![Снимок экрана (274).png](./images/Снимок%20экрана%20(274).png)
![Снимок экрана (275).png](./images/Снимок%20экрана%20(275).png)
![Снимок экрана (276).png](./images/Снимок%20экрана%20(276).png)
![Снимок экрана (277).png](./images/Снимок%20экрана%20(277).png)
![Снимок экрана (278).png](./images/Снимок%20экрана%20(278).png)
![Снимок экрана (279).png](./images/Снимок%20экрана%20(279).png)